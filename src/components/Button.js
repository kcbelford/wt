import React from 'react';

class Button extends React.Component {
  render() {
    return (
      <div>
        <button>Start</button>
        <button>End</button>
      </div>
    );
  };
}

export default Button;
